---
import type { Product } from "~/lib/api";
import { formatPrice } from "~/lib/api";
import { whatsappLink } from "~/lib/config";

interface Props {
  product: Product;
}

const { product } = Astro.props;

const orderLink = whatsappLink(
  `Hola! Quiero pedir información sobre: ${product.name} (${formatPrice(product.price)}). ¿Está disponible?`,
);
---

<article class="product-card">
  <div class="product-card-media">
    {
      product.imageUrl ? (
        <img src={product.imageUrl} alt={product.name} loading="lazy" class="h-full w-full object-cover" />
      ) : (
        <div class="h-full w-full bg-horno-creamDark" aria-hidden="true"></div>
      )
    }
  </div>

  <div class="flex flex-1 flex-col p-5">
    <h3 class="font-display text-xl leading-snug text-horno-magentaDeep">
      {product.name}
    </h3>

    <div class="mt-auto flex items-center justify-between gap-3 pt-4">
      <span class="text-2xl font-bold text-horno-magenta">
        {formatPrice(product.price)}
      </span>
      <a
        href={orderLink}
        target="_blank"
        rel="noopener"
        class="inline-flex items-center gap-2 rounded-full bg-horno-magenta px-4 py-2.5 text-sm font-semibold text-white shadow-soft transition hover:-translate-y-0.5 hover:bg-horno-magentaDeep"
      >
        Pedir
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
          <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" />
        </svg>
      </a>
    </div>
  </div>
</article>
